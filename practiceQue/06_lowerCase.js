// Qs4. Write a JavaScript program to test whether the character at the given (character)
// index is lower case.

const mystr = "divyansh"
if(mystr == mystr.toLowerCase()){
    console.log(`YES string is lower case ${mystr}`);
}
else{
    console.log(`NO string is lower case ${mystr}`);
}